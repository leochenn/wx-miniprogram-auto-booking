# 项目协作说明

1. 请始终使用中文回复。
2. 这是 Android Mock App + OpenAutoJS 抢票脚本项目。
3. Android 工程路径：`app/template-project`。
4. OpenAutoJS 脚本路径：`project`。
5. 正式脚本：`project/nanjing_booking_auto.js`。
6. Mock 测试脚本：`project/nanjing_booking_mock_app_test.js`。
7. Mock HTML 路径：`app/template-project/app/src/main/assets/booking_mock/index.html`。
8. 非必要不要修改 Gradle、`build.gradle`、`settings.gradle`。
9. 不要主动运行 `app/template-project/run_app_rtn.bat`，除非用户明确要求；用户通常会自行安装运行验证。
10. 修改正式脚本第二轮抢票链路时，必须同步检查 Mock 测试脚本是否需要保持一致。
11. 修改 Mock 测试脚本参数时，必须说明它是否仍与正式脚本第二轮参数一致。
12. 开始处理本项目任务前，请先阅读 `memory/CURRENT_CONTEXT.md`。
