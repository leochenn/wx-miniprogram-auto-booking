# OpenAutoJS 微信小程序自动预约脚本开发任务说明

你是一名经验丰富的 OpenAutoJS / Auto.js 自动化脚本开发者。请基于本说明，在 `project` 目录下开发一个可在安卓手机 OpenAutoJS 环境中运行的 JavaScript 自动化脚本，用于对微信小程序进行自动预约订票。

在正式写代码前，请先完整阅读本文档、截图目录和参考代码目录，并先输出你对需求的理解、实现方案和不确定问题。任何你不确定的地方，必须先向我提问澄清，待我明确批准后再开始编写代码。

## 1. 项目背景

我正在使用 `openautojs` 开源项目安卓 App，利用 JavaScript 脚本模拟人工点击、滑动、等待等操作，在安卓手机上操作微信小程序完成预约订票。

目标小程序桌面快捷方式名称：

`侵华日军南京大屠杀遇难同胞纪念馆参观预约`

手机和环境已准备完成：

- 安卓手机已安装 OpenAutoJS。
- 手机桌面已创建目标微信小程序快捷方式。
- 微信小程序控件无法通过无障碍控件树直接稳定读取，需要依赖截图、OCR、坐标推算和点击操作。
- 本项目工作目录中有截图和参考脚本，可用于分析页面布局和实现方式。

## 2. 目录资源

请重点查看以下目录：

- `微信小程序示例图/`：包含预约流程中各阶段的页面截图。
- `脚本代码参考/`：包含已有参考代码，其中部分逻辑已验证可用。
- `project/`：请在该目录下生成最终可运行的 JS 脚本和必要的缓存/日志辅助文件。

截图文件包括：

- `1.小程序启动后弹出预约通知.png`
- `2.同意预约通知-显示首页.png`
- `3.首页选择南京大屠杀展馆后的页面.png`
- `4.点击预约如果没有登录会弹出登录界面-登录完成后返回到首页.png`
- `5.普通预约界面-不可预约状态.png`
- `5.普通预约界面-在可预约状态下-显示选择时段.png`
- `6.预约界面-观众信息列表-3人.png`
- `6.预约界面-观众信息列表-5人.png`

参考脚本包括：

- `南京侵华-忽略隐私+登录-滑动+选择游客.js`
- `南京侵华.js`
- `定时代码测试.js`
- `测试图片识别.js`

## 3. 总体目标

开发一个自动化脚本，运行后完成两轮流程：

第一轮是预热与信息采集流程。脚本提前启动小程序，完成必要弹窗处理、登录准备、页面跳转、关键元素定位、坐标缓存和坐标推算，并最终停留在预约界面的前一个页面。

第二轮是正式抢票流程。脚本在配置的开始抢票时间到达时，尽量基于第一轮采集和缓存的坐标，快速进入预约界面并完成日期选择、时段选择、观众选择和确认预约。

正式抢票阶段应尽可能减少 OCR 调用，优先使用缓存坐标、屏幕尺寸、第一轮采集结果和相对布局推算结果，以降低耗时。

点击底部 `确认预约` 后，本次自动预约流程即视为结束。

## 4. 配置项要求

请在代码顶部预留清晰的配置区域，至少包含以下配置：

### 4.1 参观日期

配置示例：

```js
visitDay: "15"
```

取值格式为两位日号字符串，例如：

- `01`
- `02`
- `15`
- `29`
- `30`

不需要配置年月。脚本只需要在预约页面的 `参观日期` 区域内，通过 OCR 识别局部区域中的日期，并匹配指定日号，保存对应点击坐标。

要求：

- 优先识别 `参观日期` 标题/区域左侧信息，以此确定局部 OCR 区域。
- 不要全屏盲目 OCR 日期。
- 第一轮可以提前识别并缓存目标日期坐标。
- 第二轮正式抢票时优先直接点击缓存坐标。

### 4.2 选择时段

配置示例：

```js
period: "上午" // 可选值："上午"、"下午"
```

说明：

- 开票后预约页面会显示 `选择时段` 标签。
- `选择时段` 标签下方会分为左右两个区域。
- 左侧是具体上午时间段，右侧是具体下午时间段。
- 具体时间段文本不重要，不需要读取时间段内容来判断。
- 脚本应根据 `选择时段` 标签坐标，推算上午/下午两个点击区域的中心坐标。

要求：

- 第一轮未开票状态下可能看不到 `选择时段` 区域，需要结合截图和页面结构推算。
- 第二轮正式抢票时，在开票后页面出现 `选择时段` 区域，应直接点击推算出的上午/下午坐标。
- 如果推算坐标风险较高，需要在日志中明确输出依据和坐标值，便于后续验证。

### 4.3 观众信息人数

配置示例：

```js
visitorCount: 3
```

取值范围：

- `1` 到 `5`

说明：

- 配置人数与小程序中游客列表数量一致。
- 脚本需要在 `观众信息` 区域下方，从上往下点击指定数量的游客进行勾选。

要求：

- 第一轮应尽可能采集或推算游客列表区域坐标。
- 第二轮应尽量不依赖 OCR，直接根据缓存或推算坐标点击前 N 个游客。
- 由于开票前和开票后页面布局不同，第一轮需要根据未开票状态推算开票状态下 `观众信息` 标签和游客列表的位置。
- 正式抢票时，应尽量通过滑动让 `观众信息` 标签显示在靠近顶部的位置，以便快速选择 1 到 5 名游客。

### 4.4 开始抢票时间

配置示例：

```js
startTime: "17:00:00"
```

说明：

- 我会在抢票时间前至少 1 分钟运行脚本。
- 脚本启动后先执行第一轮预热与采集流程。
- 第一轮完成后，脚本等待配置时间到达。
- 配置时间到达的那一刻，立即执行第二轮正式抢票流程。

要求：

- 等待阶段需要有稳定的时间判断。
- 临近开始时间时，应减少无意义日志和耗时操作。
- 正式抢票流程应以速度优先，但必须兼顾页面跳转必要等待。

### 4.5 是否使用缓存数据

配置示例：

```js
useCache: true
```

说明：

- 如果开启缓存，脚本应读取之前保存过的元素坐标、屏幕尺寸、推算参数等数据。
- 不是简单地跳过全部采集，而是对已成功获取的数据复用，对缺失或无效的数据继续采集。

要求：

- 建议使用 JSON 文件保存缓存数据。
- 缓存文件应放在 `project` 目录或 OpenAutoJS 可写目录中，并在代码中清晰标注路径。
- 缓存数据至少应包含：屏幕宽高、关键页面标识坐标、目标日期坐标、确认预约按钮坐标、选择时段推算坐标、观众信息区域坐标、游客点击坐标、采集时间、版本号。
- 缓存读取后应校验屏幕尺寸是否一致；如果屏幕尺寸变化，缓存应视为高风险或失效。
- 每次运行应在日志中输出缓存命中、缺失、失效和重新采集情况。

## 5. OCR 和坐标策略

由于微信小程序控件无法稳定直接读取，脚本需要依赖 OCR 和图像识别，但必须控制 OCR 范围和次数。

OCR 使用要求：

- 使用 `gmlkit.ocr` 时，尽可能传入局部区域，避免全屏识别。
- 优先识别容易稳定出现的文字或区域，例如页面标题、`参观日期`、`观众信息`、`普通预约`、`确认预约`。
- 通过稳定元素坐标进行相对定位，推算其他元素坐标。
- 对 OCR 误读做容错处理，例如 `阅读并同意` 可能被识别为 `阅读井同意`。
- 文本匹配不要只依赖完全相等，应支持包含匹配、模糊匹配、常见误读兼容。

坐标策略优先级：

1. 直接使用已验证缓存坐标。
2. 使用当前屏幕尺寸和已知稳定元素坐标进行推算。
3. 使用局部 OCR 识别关键元素。
4. 必要时才使用较大范围 OCR。
5. 不建议依赖全屏 OCR 作为正式抢票阶段的主要路径。

## 6. 页面流程说明

### 6.1 启动小程序

流程：

1. 回到手机桌面。
2. 点击桌面快捷方式 `侵华日军南京大屠杀遇难同胞纪念馆参观预约`。

可能状态：

- 冷启动时，可能出现预约通知弹窗。
- 热启动时，可能直接进入首页。

冷启动弹窗处理：

- 参考截图：`1.小程序启动后弹出预约通知.png`
- 需要点击 `我已阅` 勾选。
- 然后点击 `我已知晓`。
- 之后进入首页。

首页状态：

- 参考截图：`2.同意预约通知-显示首页.png`

### 6.2 首页选择展馆

在首页点击 `南京大屠杀`。

点击后进入页面：

- 参考截图：`3.首页选择南京大屠杀展馆后的页面.png`
- 页面标题：`参观预约`
- 页面中显示：`普通预约`

### 6.3 点击普通预约

点击 `普通预约` 后可能有两种情况：

情况一：需要登录。

- 参考截图：`4.点击预约如果没有登录会弹出登录界面-登录完成后返回到首页.png`
- 需要点击 `阅读` 或类似同意项。
- 再点击微信授权/用户确认登录。
- 登录完成后可能等待约 0.5 秒或更长时间，并回到首页。
- 回到首页后需要重新进入 `南京大屠杀` 页面。

情况二：不需要登录。

- 直接进入预约界面。

要求：

- 登录流程必须做成可选分支。
- 不要假设每次都会登录，也不要假设每次都不登录。
- 页面跳转需要有限等待和重试判断。

### 6.4 预约界面状态

预约界面有两类状态。

#### 未开票/不可预约状态

参考截图：

`5.普通预约界面-不可预约状态.png`

特征：

- 有 `参观日期` 区域。
- `参观日期` 区域下方不显示 `选择时段`。
- 下方直接是 `观众信息` 区域。

第一轮预热采集时，通常会遇到该状态。

需要完成：

- 识别并保存 `参观日期` 区域位置。
- 匹配并保存目标日期坐标。
- 识别并保存 `观众信息` 区域位置。
- 识别或推算游客列表坐标。
- 识别并保存底部悬浮 `确认预约` 按钮坐标。
- 推算开票状态下 `选择时段` 区域、时段点击区域、`观众信息` 区域可能位置。

#### 开票/可预约状态

参考截图：

`5.普通预约界面-在可预约状态下-显示选择时段.png`

特征：

- 有 `参观日期` 区域。
- `参观日期` 下方显示 `选择时段`。
- `选择时段` 下方左右两侧分别是上午和下午时间段布局。
- `选择时段` 区域下方才是 `观众信息` 区域。

正式抢票时需要完成：

- 点击目标日期。
- 点击配置的上午或下午时段。
- 滑动到合适位置，让 `观众信息` 和游客列表可见。
- 从上往下勾选配置数量的游客。
- 点击底部悬浮 `确认预约`。

### 6.5 观众信息列表

参考截图：

- `6.预约界面-观众信息列表-3人.png`
- `6.预约界面-观众信息列表-5人.png`

要求：

- 根据 `visitorCount` 从上往下选择游客。
- 游客最多 5 人。
- 应尽可能通过相对布局计算游客点击坐标。
- 如果第一轮无法准确采集全部游客坐标，应输出日志说明缺失项，并在正式流程中使用可解释的推算策略。

### 6.6 确认预约按钮

底部有悬浮 `确认预约` 按钮。

特征：

- 在预约界面底部固定显示。
- 不会因页面滚动改变位置。
- 可以在第一轮提前获取坐标。

正式流程点击 `确认预约` 后，任务结束。

## 7. 两轮流程详细要求

### 7.1 第一轮：预热与采集流程

第一轮在脚本启动后立即执行。

目标：

- 回到桌面并启动小程序。
- 处理可能出现的预约通知弹窗。
- 进入首页。
- 进入 `南京大屠杀` 展馆页面。
- 点击 `普通预约`。
- 如未登录，则完成登录准备流程，并重新进入展馆页面。
- 进入预约页面，采集可采集的关键元素。
- 推算第二轮需要但当前可能不可见的坐标。
- 将采集和推算结果写入缓存。
- 最终让界面停留在预约界面的前一个页面，也就是截图 `3.首页选择南京大屠杀展馆后的页面.png` 对应页面，标题为 `参观预约`，页面中显示 `普通预约`。

第一轮结束时的页面要求：

- 必须停留在 `参观预约` 页面。
- 该页面是正式抢票时点击 `普通预约` 即可进入预约界面的页面。
- 不应停留在预约界面内部。
- 不应停留在首页。
- 不应停留在登录页。

第一轮需要尽量采集和缓存：

- 屏幕宽高。
- 首页关键入口 `南京大屠杀` 坐标。
- `普通预约` 坐标。
- 目标日期坐标。
- `参观日期` 区域坐标。
- `观众信息` 标签坐标。
- 游客列表项坐标或推算参数。
- `确认预约` 按钮坐标。
- 可预约状态下 `选择时段` 区域的推算坐标。
- 上午/下午点击坐标。
- 需要滑动到观众信息区域时的滑动起点、终点、距离或策略。

### 7.2 等待阶段

第一轮完成后，等待 `startTime` 到达。

要求：

- 日志输出当前时间、目标时间、剩余等待时间。
- 距离开始时间较远时可以低频日志。
- 临近开始时间时减少耗时操作。
- 到达目标时间后立即进入第二轮。

### 7.3 第二轮：正式抢票流程

第二轮在 `startTime` 到达时执行。

起始页面：

- 第一轮已保证停留在 `参观预约` 页面。
- 页面中有 `普通预约`。

正式流程：

1. 点击 `普通预约`。
2. 等待进入预约界面。
3. 点击目标参观日期。
4. 点击配置的上午/下午时段。
5. 滑动到 `观众信息` 和游客列表合适位置。
6. 从上往下点击选择 `visitorCount` 个游客。
7. 点击底部悬浮 `确认预约`。
8. 输出完整日志并结束。

正式流程性能要求：

- 尽可能使用缓存和推算坐标。
- 不要在正式流程中做大量全屏 OCR。
- 必要等待应短而可靠。
- 每个关键动作前后都应输出耗时日志。

## 8. 日志要求

日志非常重要。脚本运行完成后，我会把完整日志发给你，用于复盘、排查、定位和修复问题。

日志必须能还原完整流程。

至少记录：

- 脚本启动时间。
- 配置项内容。
- 屏幕宽高。
- 是否使用缓存。
- 缓存文件路径。
- 缓存命中、缺失、失效、重新采集情况。
- 当前所处阶段：第一轮、等待阶段、第二轮。
- 每次 OCR 的区域、识别到的文本摘要、耗时、匹配结果。
- 每次点击的目标名称和坐标。
- 每次滑动的起点、终点、耗时。
- 每次页面跳转等待的开始时间、结束时间、耗时、判断依据。
- 每个关键元素的最终坐标来源：缓存、OCR、推算、默认配置。
- 第二轮正式抢票各步骤耗时。
- 异常、重试、降级策略和最终失败原因。

建议日志格式：

```text
[2026-xx-xx 16:58:01.123][PREP][OCR] 查找“参观日期” region={x,y,w,h} cost=86ms result=...
[2026-xx-xx 16:58:01.245][PREP][CACHE] targetDate 坐标写入 x=...
[2026-xx-xx 17:00:00.002][RUSH][CLICK] 普通预约 x=... y=...
```

## 9. 容错和重试要求

必须处理以下不稳定情况：

- 小程序冷启动或热启动状态不同。
- 预约通知弹窗可能出现，也可能不出现。
- 登录弹窗可能出现，也可能不出现。
- 页面跳转耗时不稳定。
- OCR 文本可能误读。
- 页面局部区域可能短时间未渲染完成。
- 缓存坐标可能缺失或失效。
- 未开票页面和开票页面布局不同。

建议策略：

- 每个页面判断都设置有限重试和超时。
- 页面未识别时，不要无限等待。
- OCR 匹配支持常见误读。
- 点击后等待应结合页面识别，而不是固定长时间 sleep。
- 关键动作失败时输出明确错误日志。
- 如果正式抢票阶段某个缓存坐标缺失，应使用局部 OCR 或推算作为降级方案。

## 10. 建议实现结构

请尽量写成结构清晰、可维护、可反复调试的脚本，而不是把所有逻辑写成一长段。

建议模块/函数：

- 配置区：集中管理 `visitDay`、`period`、`visitorCount`、`startTime`、`useCache`。
- 日志模块：统一输出带时间戳、阶段、动作类型的日志。
- 缓存模块：读取、校验、合并、写入缓存。
- OCR 模块：封装区域 OCR、模糊匹配、耗时统计。
- 坐标模块：保存关键坐标、推算时段和游客坐标。
- 页面识别模块：判断首页、展馆页、登录页、预约页、通知弹窗。
- 第一轮流程：`prepareFlow()`。
- 等待流程：`waitUntilStartTime()`。
- 第二轮流程：`rushFlow()`。
- 异常处理：统一捕获并输出最终上下文。

## 11. 开发协作要求

请不要试图一次性假设所有坐标都完全正确。

推荐开发方式：

1. 先阅读截图和参考代码。
2. 输出你对需求的完整理解。
3. 输出你计划采用的 OCR 区域、坐标推算策略和缓存结构。
4. 明确列出不确定点，让我确认。
5. 待我批准后，再开始写代码。
6. 第一版代码优先实现完整流程、日志和缓存框架。
7. 对于坐标推算不确定的地方，在日志中输出详细依据，便于我运行后反馈。
8. 根据我提供的运行日志和截图反馈，继续迭代修复。

## 12. 验收标准

第一阶段验收：

- 能运行脚本。
- 能输出完整日志。
- 能启动小程序并处理通知弹窗。
- 能进入首页和展馆页面。
- 能处理可能出现的登录流程。
- 能进入预约页面并采集关键元素。
- 能写入缓存。
- 第一轮结束后能停留在 `参观预约` 页面。

第二阶段验收：

- 能按配置时间等待并准时触发第二轮。
- 第二轮能点击 `普通预约` 进入预约页面。
- 能按配置点击目标日期。
- 能按配置点击上午或下午时段。
- 能按配置选择 1 到 5 个游客。
- 能点击底部 `确认预约`。
- 能输出足够详细的耗时和坐标来源日志。

最终目标：

- 在正式抢票流程中，尽量少依赖 OCR。
- 第二轮流程尽可能极速执行。
- 出现失败时，日志足够完整，可以根据日志复原流程并定位问题。
